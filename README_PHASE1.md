# DB-BenchMind

**桌面压测工作台** - 面向数据库工程师和性能测试工程师的统一 GUI 工具

## 功能特性

- ✅ **连接管理**：支持 MySQL、Oracle、SQL Server、PostgreSQL
- ✅ **模板管理**：7 个内置压测模板
- 🚧 **任务编排**：配置和执行压测任务
- 🚧 **实时监控**：TPS、延迟、错误率实时显示
- 🚧 **结果对比**：多次运行结果对比分析
- 🚧 **报告导出**：MD/HTML/JSON/PDF 格式

## 快速开始

### 环境要求

- Go 1.22.2+
- Ubuntu 24 (带桌面 GUI)
- SQLite 3 (自带)

### 安装

```bash
# 克隆仓库
git clone https://github.com/whhaicheng/DB-BenchMind.git
cd DB-BenchMind

# 下载依赖
go mod download

# 运行（需要 GUI 环境）
go run cmd/db-benchmind/main.go
```

### 测试

```bash
# 运行所有测试
make test

# 仅单元测试
make test-unit

# 集成测试
make test-integration
```

### 构建

```bash
make build
```

## 项目结构

```
DB-BenchMind/
├── cmd/db-benchmind/       # GUI 入口
├── internal/
│   ├── app/usecase/        # 用例编排
│   ├── domain/             # 领域模型
│   │   ├── connection/     # 连接领域
│   │   ├── execution/      # 执行领域
│   │   └── metric/         # 指标领域
│   ├── infra/              # 基础设施
│   │   ├── database/       # SQLite 实现
│   │   ├── keyring/        # 密钥管理
│   │   └── adapter/        # 工具适配器
│   └── transport/ui/       # GUI 界面
├── test/integration/        # 集成测试
└── .specify/steering/      # 项目记忆
```

## 架构设计

项目采用 **Clean Architecture + DDD** 分层架构：

- **Domain Layer**: 核心业务逻辑，无外部依赖
- **Application Layer**: 用例编排，定义接口
- **Infrastructure Layer**: 外部依赖实现
- **Transport Layer**: GUI 界面

详见 `.specify/steering/architecture.md`

## 测试覆盖

当前测试覆盖率：

| 包 | 单元测试 | 集成测试 |
|----|---------|---------|
| domain/connection | ✅ 13 tests | - |
| infra/database | ✅ 6 tests | - |
| infra/database/repository | ✅ 6 tests | - |
| infra/keyring | ✅ 6 tests | - |
| app/usecase | ✅ 7 tests | - |
| test/integration | - | ✅ 6 tests |
| **总计** | **38 tests** | **6 tests** |

所有测试通过 ✅

## 开发状态

### Phase 1: 基础设施与连接管理（已完成）

- ✅ SQLite 数据库设计与初始化
- ✅ 四种数据库连接模型
- ✅ ConnectionRepository 接口与实现
- ✅ 密钥管理
- ✅ ConnectionUseCase 业务逻辑
- ✅ 项目文档
- ✅ 集成测试

### Phase 2-8: 待开发

- 🚧 模板系统与任务配置
- 🚧 工具适配器与执行编排
- 🚧 结果存储与历史记录
- 🚧 报告生成与导出
- 🚧 结果对比功能
- 🚧 设置页面与优化
- 🚧 文档与测试完善

## 依赖说明

### 当前依赖

- `modernc.org/sqlite` - 纯 Go SQLite 驱动
- `github.com/google/uuid` - UUID 生成

### 可选依赖（需要时安装）

- GUI: `fyne.io/fyne/v2` - Fyne GUI 框架
- 系统密钥: `github.com/zalando/go-keyring` - 密钥管理

**注意**：由于网络问题，Fyne 和 go-keyring 暂时使用 stub 实现。完整功能需要添加这些依赖。

## 配置

### 数据库配置

默认数据库路径：`./data/db-benchmind.db`

可通过环境变量或配置文件修改（TODO: Phase 7）

### 密钥管理

- 优先使用系统 keyring（gnome-keyring）
- 降级到加密文件（`./data/` 目录）

## 许可证

[待添加]

## 贡献指南

[待添加]

## 联系方式

[待添加]
