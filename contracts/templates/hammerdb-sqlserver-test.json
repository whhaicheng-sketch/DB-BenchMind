{
  "$schema": "https://db-benchmind.dev/schemas/template/v1.json",
  "id": "hammerdb-sqlserver-test",
  "name": "Test (HammerDB)",
  "description": "Lightweight test template for quick SQL Server testing (1 warehouse, minimal dataset, rampup time configurable in tasks)",
  "tool": "hammerdb",
  "database_types": ["sqlserver"],
  "version": "1.0.0",
  "parameters": {
    "threads": {
      "type": "integer",
      "label": "Number of virtual users",
      "default": 4,
      "min": 1,
      "max": 1000
    },
    "warehouses": {
      "type": "integer",
      "label": "Number of warehouses",
      "default": 1,
      "min": 1,
      "max": 1000
    },
    "rampup": {
      "type": "integer",
      "label": "Ramp up time (minutes)",
      "default": 0,
      "min": 0,
      "max": 60
    }
  },
  "command_template": {
    "prepare": "hammerdbcli autobuild \"dbset db mssqls; dbset bm TPC-C; diset connection mssqls_linux_server {host}; diset connection mssqls_tcp true; diset connection mssqls_port {port}; diset connection mssqls_uid {user}; diset connection mssqls_pass {password}; diset tpcc mssqls_count_ware {warehouses}; buildschema\"",
    "run": "hammerdbcli autobuild \"dbset db mssqls; dbset bm TPC-C; diset connection mssqls_linux_server {host}; diset connection mssqls_tcp true; diset connection mssqls_port {port}; diset connection mssqls_uid {user}; diset connection mssqls_pass {password}; diset tpcc mssqls_driver timed; diset tpcc mssqls_rampup {rampup}; diset tpcc mssqls_duration {rampup}; vuset vu {threads}; vucreate; vurun; vudestroy\"",
    "cleanup": "hammerdbcli autobuild \"dbset db mssqls; dbset bm TPC-C; diset connection mssqls_linux_server {host}; diset connection mssqls_tcp true; diset connection mssqls_port {port}; diset connection mssqls_uid {user}; diset connection mssqls_pass {password}; dropschema\""
  },
  "output_parser": {
    "type": "regex",
    "patterns": {
      "tpm": "TPM\\s*:\\s*(\\d+\\.?\\d*)",
      "nopm": "NOPM\\s*:\\s*(\\d+\\.?\\d*)",
      "efficiency": "Efficiency\\s*:\\s*(\\d+\\.?\\d*)%",
      "errors": "Errors\\s*:\\s*(\\d+)"
    }
  }
}
