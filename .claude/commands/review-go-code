---
description: 审查指定的 Go 代码文件或目录；结合 constitution.md（若存在）与静态检查结果给出结构化反馈。
argument-hint: [path_to_review]
model: opus
allowed-tools: Read, Grep, Glob, Bash(go env:*, go list:*, go test:*, go vet:*, gofmt:*, golangci-lint:*, staticcheck:*, govulncheck:*)
---

你现在是本项目的 Go 代码审查官。你的任务是审查 `@$1` 指定路径下的 Go 代码（文件或目录），并生成**可执行、可验证**的审查报告。

## 0) 审查范围与前置说明
- 审查目标路径：`$1`（为空则默认为 `.`）
- 若仓库根目录存在 `constitution.md`：必须将其作为最高优先级的审查依据；否则遵循本指令内的“通用 Go 审查标准”。

## 1) 收集上下文（必须）
1. 识别 Go module 根目录（含 go.mod 的目录）与模块信息：
   - `go env GOMOD`
   - `go list -m`
2. 读取 `constitution.md`（若存在）与项目约定文件（若存在）：
   - `constitution.md`
   - `CLAUDE.md` / `README.md` / `.golangci.yml`

## 2) 自动化检查（尽力而为：能跑则跑，不能跑则说明原因）
在 module 根目录执行（如路径在子目录，也需切回 module 根执行）：
- gofmt（只检查，不自动改）：
  - `gofmt -l $1`
- go vet：
  - `go vet $1`（或 `go vet ./...`，视项目结构选择）
- go test：
  - `go test ./...`
- 可选（若工具存在/项目启用）：
  - `golangci-lint run`
  - `staticcheck ./...`
  - `go test ./... -race`（当代码涉及并发时强烈建议）
  - `govulncheck ./...`（依赖安全）

> 将所有命令的输出摘要写入“静态检查结果”章节；若某命令无法执行，必须说明“缺少工具/耗时过长/项目未配置”等原因。

## 3) 深入审查维度（必须）
围绕以下维度逐条审查，并给出结论与证据（文件+行号）：

### 3.1 简单性（Simplicity）
- 是否存在不必要抽象/过度分层/为了未来的框架化设计？
- 是否能用标准库完成却引入了不必要依赖？
- 是否存在“重复造轮子”的通用工具包？

### 3.2 明确性（Clarity / Explicitness）
- 错误是否显式处理？是否存在 `_ = fn()` 丢弃错误？
- 错误是否按项目约定 wrap（如 `fmt.Errorf("...: %w", err)`）并携带操作上下文？
- 依赖是否显式注入（参数/struct），避免全局状态？
- 日志是否结构化（若项目要求），上下文字段是否一致？

### 3.3 单一职责（Single Responsibility）
- 包/文件/函数职责是否单一？是否出现“既做 IO 又做业务又做格式化”的混杂？
- internal 分层是否清晰？cmd 是否只做装配？
- 是否把外部 SDK/HTTP 框架类型泄漏到核心逻辑？

### 3.4 Go 特有高频问题（通用加分项）
- context 是否正确传播（ctx 作为第一个参数、取消/超时传递、避免泄漏）？
- 并发是否安全（共享状态、锁粒度、goroutine 泄漏、channel 关闭时机）？
- 性能/内存是否存在明显问题（热路径分配、fmt 拼接、无界缓存/worker）？

## 4) 行号与证据要求（强制）
- 对每一个问题，必须给出：**文件名、精确行号、问题描述、修改建议**。
- 行号必须可验证：用 `nl -ba <file> | sed -n 'L,Mp'` 或等价方式获取，不得猜测。

## 5) 输出格式（必须，Markdown）
- 总体评价（一句话）
- 优点（1~3 条）
- 待改进项（按优先级：高/中/低）
  - 每条包括：位置（file:line）、影响、建议、（可选）示例代码
- （可选）建议的重构拆分方案（仅在确有必要且不违背“简单性”时）

